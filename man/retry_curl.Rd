% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extr_comptox.R, R/other.R
\name{retry_curl}
\alias{retry_curl}
\title{Retry wrapper for curl (via condathis::run)}
\usage{
retry_curl(
  args,
  url,
  env_name = "openssl-linux-env",
  max_attempts = 4L,
  initial_wait = 1,
  backoff_factor = 2,
  jitter = 0.2,
  retry_on_status = c(429, 500:599, 404),
  check_success = function(res, outfile = NULL) TRUE,
  outfile = NULL
)

retry_curl(
  args,
  url,
  env_name = "openssl-linux-env",
  max_attempts = 4L,
  initial_wait = 1,
  backoff_factor = 2,
  jitter = 0.2,
  retry_on_status = c(429, 500:599, 404),
  check_success = function(res, outfile = NULL) TRUE,
  outfile = NULL
)
}
\arguments{
\item{args}{Character vector of curl arguments that come before the URL.}

\item{url}{Target URL (string).}

\item{env_name}{Name of the conda environment to pass to condathis::run.}

\item{max_attempts}{Integer: maximum retry attempts (default 4).}

\item{initial_wait}{Numeric: initial wait (seconds) for backoff (default 1).}

\item{backoff_factor}{Numeric: exponential backoff factor (default 2).}

\item{jitter}{Numeric: fractional jitter applied to waits (default 0.2).}

\item{retry_on_status}{Integer vector of HTTP status codes considered retryable.
Defaults to c(429, 500:599, 404).}

\item{check_success}{Function(result, outfile) -> logical. Called with a parsed
result list (stdout, stderr, http_code) and optional outfile path; should
return TRUE when the attempt is considered successful.}

\item{outfile}{Optional path to an output file (used by check_success).}
}
\value{
A list with fields: success, result, attempts, http_code, message.

A list with fields:
\itemize{
\item success: logical
\item result: raw condathis::run return (or error object)
\item attempts: integer number of attempts performed
\item http_code: parsed HTTP status code (if available)
\item message: failure message when success == FALSE
}
}
\description{
Run the curl binary (through condathis::run) with retry/backoff logic.

Run the curl binary (through condathis::run) with retry/backoff logic.
Designed for use when you must call the curl CLI rather than an HTTP client
library. Returns a structured list with success, attempts and the raw result.
}
